$(document).ready(function() {
 errorloginalert = $('#ErrorAuthLoginAlert')
 errorloginalert.hide();
/* $('#login_btn').on('click', function (e) {
     e.preventDefault();
     var username = $('#AuthUserName').val();
     var password = $('#AuthPassword').val();
     $.ajax({
         url: '/ajax/validate_authorization',
         data: {
             'username': username,
             'password': password,
         },
         datatype: 'json',
         success: function (data) {
           if(data.is_user)
            {
              console.log(data)
            }
         }
     })
 })*/
})